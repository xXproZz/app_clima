name: app_clima
description: Um aplicativo de clima que busca dados de uma API.

publish_to: 'none' # Remove esta linha se você quiser publicar o pacote

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.3

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true